specVersion: "v1"
serviceId: "closeby"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "closeby-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
